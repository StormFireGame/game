var React = require('react');
var debug = require('debug')('game:pages:hero:preferences');

var HeroNewPage = React.createClass({
  render: function() {
    debug('render');

    return (
      <div>
        preferences
      </div>
    );
  }
});

module.exports = HeroNewPage;
